#include "main.h"
#include <string.h>
/**
 * _strcmp - compares strings
 *
 * @s1: first string
 * @s2: second string
 *
 * Return: 0 - identical, more or less - not identical
 */
int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
