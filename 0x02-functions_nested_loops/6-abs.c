#include "main.h"
#include <stdlib.h>
/**
 * _abs - checks and makes all numbers absolute
 *
 * @n: parameter for checking absolutidity
 *
 * Return: Returns the absolute value
 */
int _abs(int n)
{
	int absolute = abs(n);

	return (absolute);
}
