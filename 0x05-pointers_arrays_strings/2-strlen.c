#include "main.h"
#include <string.h>
/**
 * _strlen - counts characters
 * can't believe we are using pointers
 * @s: parameter for a string
 *
 * Return: integer size of the string
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
